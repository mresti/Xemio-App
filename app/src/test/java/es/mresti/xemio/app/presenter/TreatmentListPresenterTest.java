package es.mresti.xemio.app.presenter;

import com.firebase.client.Firebase;
import es.mresti.xemio.app.contract.TreatmentListContract;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.when;

public class TreatmentListPresenterTest {

  //@Mock private Firebase mFirebase;
  //
  //@Mock private TreatmentListContract.View mTreatmentListView;
  //
  //private TreatmentListPresenter mTreatmentListPresenter;
  //
  //@Before public void setupTreatmentListPresenter() {
  //  // Mockito has a very convenient way to inject mocks by using the @Mock annotation. To
  //  // inject the mocks in the test the initMocks method needs to be called.
  //  MockitoAnnotations.initMocks(this);
  //
  //  // Get a reference to the class under test
  //  mTreatmentListPresenter = new TreatmentListPresenter(mTreatmentListView);
  //}
  //
  //@Test public void getTreatmentListRef_Firebase() {
  //  Firebase firebase = mTreatmentListPresenter.getTreatmentListRef();
  //
  //  when(firebase.getRef()).thenReturn(mFirebase);
  //}
}
