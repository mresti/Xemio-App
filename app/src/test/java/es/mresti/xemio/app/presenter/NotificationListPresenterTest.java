package es.mresti.xemio.app.presenter;

import com.firebase.client.Firebase;
import es.mresti.xemio.app.contract.NotificationListContract;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.when;

public class NotificationListPresenterTest {

  //@Mock private Firebase mFirebase;
  //
  //@Mock private NotificationListContract.View mNotificationListView;
  //
  //private NotificationListPresenter mNotificationListPresenter;
  //
  //@Before public void setupNotificationListPresenter() {
  //  // Mockito has a very convenient way to inject mocks by using the @Mock annotation. To
  //  // inject the mocks in the test the initMocks method needs to be called.
  //  MockitoAnnotations.initMocks(this);
  //
  //  // Get a reference to the class under test
  //  mNotificationListPresenter = new NotificationListPresenter(mNotificationListView);
  //}
  //
  //@Test public void getNotificationListRef_Firebase() {
  //  Firebase firebase = mNotificationListPresenter.getNotificationListRef();
  //
  //  when(firebase.getRef()).thenReturn(mFirebase);
  //}
}
