package es.mresti.xemio.app.presenter;

import com.firebase.client.Firebase;
import es.mresti.xemio.app.contract.HistoricalListContract;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.when;

public class HistoricalListPresenterTest {

  //@Mock private Firebase mFirebase;
  //
  //@Mock private HistoricalListContract.View mHistoricalListView;
  //
  //private HistoricalListPresenter mHistoricalListPresenter;
  //
  //@Before public void setupHistoricalListPresenter() {
  //  // Mockito has a very convenient way to inject mocks by using the @Mock annotation. To
  //  // inject the mocks in the test the initMocks method needs to be called.
  //  MockitoAnnotations.initMocks(this);
  //
  //  // Get a reference to the class under test
  //  mHistoricalListPresenter = new HistoricalListPresenter(mHistoricalListView);
  //}
  //
  //@Test public void getHistoricalListRef_Firebase() {
  //  Firebase firebase = mHistoricalListPresenter.getHistoricalListRef();
  //
  //  when(firebase.getRef()).thenReturn(mFirebase);
  //}
}
