package es.mresti.xemio.app.presenter;

import com.firebase.client.Firebase;
import es.mresti.xemio.app.contract.HistoricalDetailContract;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.when;

public class HistoricalDetailPresenterTest {

  //@Mock private Firebase mFirebase;
  //
  //@Mock private HistoricalDetailContract.View mHistoricalDetailView;
  //
  //private HistoricalDetailPresenter mHistoricalDetailPresenter;
  //
  //@Before public void setupHistoricalDetailPresenter() {
  //  // Mockito has a very convenient way to inject mocks by using the @Mock annotation. To
  //  // inject the mocks in the test the initMocks method needs to be called.
  //  MockitoAnnotations.initMocks(this);
  //
  //  // Get a reference to the class under test
  //  mHistoricalDetailPresenter = new HistoricalDetailPresenter(mHistoricalDetailView);
  //}
  //
  //@Test public void getAdviceRef_Firebase() {
  //  Firebase firebase = mHistoricalDetailPresenter.getHistoricalRef("myKey");
  //
  //  when(firebase.getRef()).thenReturn(mFirebase);
  //}
}
