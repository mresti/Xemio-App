package es.mresti.xemio.app.presenter;

import com.firebase.client.Firebase;
import es.mresti.xemio.app.contract.TreatmentDetailContract;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.when;

public class TreatmentDetailPresenterTest {

  //@Mock private Firebase mFirebase;
  //
  //@Mock private TreatmentDetailContract.View mTreatmentDetailView;
  //
  //private TreatmentDetailPresenter mTreatmentDetailPresenter;
  //
  //@Before public void setupTreatmentDetailPresenter() {
  //  // Mockito has a very convenient way to inject mocks by using the @Mock annotation. To
  //  // inject the mocks in the test the initMocks method needs to be called.
  //  MockitoAnnotations.initMocks(this);
  //
  //  // Get a reference to the class under test
  //  mTreatmentDetailPresenter = new TreatmentDetailPresenter(mTreatmentDetailView);
  //}
  //
  //@Test public void getTreatmentRef_Firebase() {
  //  Firebase firebase = mTreatmentDetailPresenter.getTreatmentRef("myKey");
  //
  //  when(firebase.getRef()).thenReturn(mFirebase);
  //}
}
