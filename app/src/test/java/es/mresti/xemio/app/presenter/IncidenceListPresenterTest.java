package es.mresti.xemio.app.presenter;

import com.firebase.client.Firebase;
import es.mresti.xemio.app.contract.IncidenceListContract;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.mockito.Mockito.when;

public class IncidenceListPresenterTest {

  //@Mock private Firebase mFirebase;
  //
  //@Mock private IncidenceListContract.View mIncidenceListView;
  //
  //private IncidenceListPresenter mIncidenceListPresenter;
  //
  //@Before public void setupIncidenceListPresenter() {
  //  // Mockito has a very convenient way to inject mocks by using the @Mock annotation. To
  //  // inject the mocks in the test the initMocks method needs to be called.
  //  MockitoAnnotations.initMocks(this);
  //
  //  // Get a reference to the class under test
  //  mIncidenceListPresenter = new IncidenceListPresenter(mIncidenceListView);
  //}
  //
  //@Test public void getIncidenceListRef_Firebase() {
  //  Firebase firebase = mIncidenceListPresenter.getIncidenceListRef();
  //
  //  when(firebase.getRef()).thenReturn(mFirebase);
  //}
}
